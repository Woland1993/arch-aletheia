#!/bin/bash

# Toggle microphone mute. Dell XPS systems get special handling for the hardware LED.

if aletheia-hw-match "XPS"; then
  aletheia-cmd-mic-mute-xps
else
  swayosd-client --monitor "$(aletheia-hyprland-monitor-focused)" --input-volume mute-toggle
fi
